help = mole test

[arg]
download-dir|dir = ''
yaml = test

[arg2env]
mole.dir = download-dir
tidb.tiup.yaml = yaml

[env]
bench.workload =read

[flow/]
db.conf.test cluster=test-mole : mole.precheck :  db.rm : db.new :
[[bench.workload]].load : bench.run : mole.collect : mole.features : env.reset
